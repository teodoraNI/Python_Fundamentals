# List = [[4, 3, 2], [16, 1, 4, 64], [6, 3, 9, 12]]
#
# # Sort each sublist
# sortList = lambda x: (sorted(i) for i in x)
#
# # Get the second largest element
# secondLargest = lambda x, f: [y[len(y) - 2] for y in f(x)]
# res = secondLargest(List, sortList)
#
# print(res)
